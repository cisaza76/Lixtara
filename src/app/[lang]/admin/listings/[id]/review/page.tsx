import { AdminComingSoon } from "@/components/admin/admin-coming-soon";

export default function Page() {
  return (
    <AdminComingSoon
      title="Listing Review"
      note="The full broker review screen (approve / request changes / reject / sign as broker, with photos, price comparison and compliance checklist) ships in the next phase."
    />
  );
}
